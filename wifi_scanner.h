#ifndef WIFI_SCANNER_H
#define WIFI_SCANNER_H

#include <vector>
#include <string>

// Объявляем функцию, которую можно будет вызвать извне
std::vector<std::string> get_wifi_networks();

#endif